import { makeStyles } from '@material-ui/core/styles';

export const signInButtonStyles = makeStyles({
    signInUrl: {
        color: 'white',
        fontSize: '1.3rem',
        textDecoration: 'none',
        cursor: 'pointer'
    },
  });